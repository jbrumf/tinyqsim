name: tinyqsim
channels:
  - defaults
dependencies:
  - python>=3.12
  - more-itertools
  - numpy>=2
  - scipy
  - matplotlib
  - notebook
  - ipython
  - ipympl
  - pytest
  - pip
  - pip:
      - pdoc
      - nb-clean
